{% extends 'LIHotelBundle::adminBase.html.twig' %}

{% block body -%}
<div class="col-lg-12 col-sm-12 col-xs-12">
    <div class="page-header">
        <h1>Habitacion {{ entity.nombre }} (#{{ entity.numero }})</h1>
    </div>

    <table class="table table-bordered record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ entity.estado }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.numero }} - {{ entity.nombre }}</td>
            </tr>
            <tr>
                <th>Categoria</th>
                <td>{{ entity.tipo.categoriaHabitacion }}</td>
            </tr>
            <tr><th>Tipo</th><td>{{ entity.tipo.tipoHabitacion }}</td></tr>
            <tr><th>Espacio Interno</th><td>{{ entity.tipo.espacioInterno }}</td></tr>
            <tr><th>Servicios</th><td>{{ entity.tipo.servicios }}</td></tr>
            <tr>
                <table class="table">
                    <thead>
                        <tr>
                            <th>Bebidas del Minibar</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for bebida in entity.tipo.bebidasMinibar %}
                            <tr>
                                <td>{{ bebida.marca }}</td>
                                <td>{{ bebida.cantidad }} unidades</td>
                                <td>{{ bebida.precio }} Bs</td>
                                <td>{{ bebida.tipoBebida }} unidades</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>   
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('habitacion') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('habitacion_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
</div>
{% endblock %}
