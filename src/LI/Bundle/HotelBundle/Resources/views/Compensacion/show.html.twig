{% extends 'LIHotelBundle::adminBase.html.twig' %}

{% block body -%}
<div class="col-lg-12 col-sm-12 col-xs-12">
    <div class="page-header">
        <h1>Compensacion</h1>
    </div>
    <table class="table table-bordered table-striped record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Porcentaje</th>
                <td>{{ entity.porcentaje }}</td>
            </tr>
            <tr>
                <th>Dedias</th>
                <td>{{ entity.deDias }}</td>
            </tr>
            <tr>
                <th>Adias</th>
                <td>{{ entity.aDias }}</td>
            </tr>
            <tr>
                <th>Dehoras</th>
                <td>{{ entity.deHoras }}</td>
            </tr>
            <tr>
                <th>Ahoras</th>
                <td>{{ entity.aHoras }}</td>
            </tr>
        </tbody>
    </table>

    <div class="row">
        <div class="col-lg-4 col-sm-4 col-xs-12"><a class="btn btn-success btn-block" href="{{ path('compensacion') }}" role="button">Regresar</a></div>
        <div class="col-lg-4 col-sm-4 col-xs-12"><a class="btn btn-warning btn-block" href="{{ path('compensacion_edit', { 'id': entity.id }) }}" role="button">Editar Compensación</a></div>
        <div class="col-lg-4 col-sm-4 col-xs-12">{{ form(delete_form) }}</div>
    </div>
</div>
{% endblock %}
