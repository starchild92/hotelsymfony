{% extends '::base.html.twig' %}

{% block body -%}
    <h1>OcupacionHabitacion list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Categoriahabitacion</th>
                <th>Tipohabitacion</th>
                <th>Cantidadpersonashabitacion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('ocupacion_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.categoriaHabitacion }}</td>
                <td>{{ entity.tipoHabitacion }}</td>
                <td>{{ entity.cantidadPersonasHabitacion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('ocupacion_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('ocupacion_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('ocupacion_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
