{% extends 'LIHotelBundle::adminBase.html.twig' %}

{% block body -%}
    <div class="page-header">
        <h1>Modificar Ocupación Habitación</h1>
    </div>

    <div class="row">
        <div class="col-lg-3 col-sm-3 col-xs-12">
            <ul class="nav nav-stacked">
                <li role="presentation"><a class="btn btn-success btn-block" href="{{ path('ocupacionhabitacion') }}" role="button">Ver todas las Ocupaciones</a></li>
                <li role="presentation">{{ form(delete_form) }}</li>
                <li role="presentation">{{ form(delete_form) }}</li>
            </ul>
        </div>

        <!-- PANEL DEL CONTENIDO -->
        <div class="col-lg-9 col-sm-9 col-xs-12">
            {% for message in app.session.flashbag.get('ocupacion_malos') %}
            <div class="alert alert-danger" role="alert"><b>Oh crap! </b>{{ message }}</div>
            {% endfor %}
            {% for message in app.session.flashbag.get('ocupacion_buenos') %}
                <div class="alert alert-success" role="alert"><b>En horabuena! </b>{{ message }}</div>
            {% endfor %}

            {{ form_start(edit_form) }}
                {{ form_errors(edit_form) }}
            {{ form_end(edit_form) }}
        </div>
    </div>


    <div class="row">
        
        
        
    </div>
{% endblock %}
