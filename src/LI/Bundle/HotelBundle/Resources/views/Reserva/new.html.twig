{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Reserva creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('reserva') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
